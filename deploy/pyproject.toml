[project]
name = "pulumi-events-infra"
version = "0.1.0"
description = "Pulumi IaC for deploying pulumi-events MCP server to AWS"
requires-python = ">=3.12"
dependencies = [
    "pulumi>=3.228.0",
    "pulumi-aws>=7.24.0",
    "pulumi-awsx>=3.3.1",
    "pulumi-docker-build>=0.0.15",
]
